// Product name in one place so it can be changed without hunting through components.
export const SITE_NAME = 'Monospace';
export const SITE_TAGLINE = 'Technical writing by developers, for developers.';

export const POSTS_PER_PAGE = 10;
export const MAX_TAGS_PER_POST = 5;
